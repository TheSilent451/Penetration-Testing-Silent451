1. Ram mem speed check : wmic memorychip get speed

2. Installation without admin privilege
   // Save this in bat extention
   // Set _COMPACT_LAYER=RunAsInvoker
   // Start SteamSetup.exe
3.Linux
   // add user - sudo adduser <username>
   // nano /etc/sudoers - add [ root  ALL=(ALL:ALL) ALL ]

4. [Test Network Connection ]  tnc example.com -port 22

5. You can create a new GUID in PowerShell using the following command: [guid]::NewGuid()

6. to check the hash value of file : certutil -hashfile C:\Users\rk\Desktop\js.rar SHA256

6. for ssl cypher suit scan : nmap --script ssl-enum-ciphers -p -443 www.example.com

7. nikto -h example.com -o report.html -Format htm



HTML Ijection : 

import org.apache.commons.text.StringEscapeUtils;
public class HTMLInjectionExample {
 
 public static void main(String[] args) {
 // Simulating user input
 String userInput = "<script>alert('Hello!');</script>";
// Incorrect implementation - Concatenating input without escaping
 String incorrectOutput = "<p>Welcome, " + userInput + "!</p>";
 System.out.println("Incorrect Output: " + incorrectOutput);
// Correct implementation - Escaping user input
 String sanitizedInput = StringEscapeUtils.escapeHtml4(userInput);
 String correctOutput = "<p>Welcome, " + sanitizedInput + "!</p>";
 System.out.println("Correct Output: " + correctOutput);
 }
}
